import { del as del$1, get as get$1, put } from './raw.js';

const get = (http, params) => {
    return get$1(http, `/organizations/${params.organizationId}/app_definitions/${params.appDefinitionId}/event_subscription`);
};
const upsert = (http, params, data) => {
    return put(http, `/organizations/${params.organizationId}/app_definitions/${params.appDefinitionId}/event_subscription`, data);
};
const del = (http, params) => {
    return del$1(http, `/organizations/${params.organizationId}/app_definitions/${params.appDefinitionId}/event_subscription`);
};

export { del, get, upsert };
//# sourceMappingURL=app-event-subscription.js.map
