import { del as del$1, get as get$1, put } from './raw.js';

const get = (http, params) => {
    return get$1(http, `/organizations/${params.organizationId}/app_definitions/${params.appDefinitionId}/signing_secret`);
};
const upsert = (http, params, data) => {
    return put(http, `/organizations/${params.organizationId}/app_definitions/${params.appDefinitionId}/signing_secret`, data);
};
const del = (http, params) => {
    return del$1(http, `/organizations/${params.organizationId}/app_definitions/${params.appDefinitionId}/signing_secret`);
};

export { del, get, upsert };
//# sourceMappingURL=app-signing-secret.js.map
