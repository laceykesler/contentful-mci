import { post } from './raw.mjs';

const create = (http, params, data) => {
    return post(http, `/spaces/${params.spaceId}/environments/${params.environmentId}/app_installations/${params.appDefinitionId}/signed_requests`, data);
};

export { create };
//# sourceMappingURL=app-signed-request.mjs.map
