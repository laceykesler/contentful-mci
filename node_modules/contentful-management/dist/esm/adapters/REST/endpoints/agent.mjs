import { post, get as get$1 } from './raw.mjs';

const AgentAlphaHeaders = {
    'x-contentful-enable-alpha-feature': 'agents-api',
};
const get = (http, params, headers) => {
    return get$1(http, `/spaces/${params.spaceId}/environments/${params.environmentId}/ai/agents/${params.agentId}`, {
        headers: {
            ...AgentAlphaHeaders,
            ...headers,
        },
    });
};
const getMany = (http, params, headers) => {
    return get$1(http, `/spaces/${params.spaceId}/environments/${params.environmentId}/ai/agents`, {
        headers: {
            ...AgentAlphaHeaders,
            ...headers,
        },
    });
};
const generate = (http, params, data, headers) => {
    return post(http, `/spaces/${params.spaceId}/environments/${params.environmentId}/ai/agents/${params.agentId}/generate`, data, {
        headers: {
            ...AgentAlphaHeaders,
            ...headers,
        },
    });
};

export { generate, get, getMany };
//# sourceMappingURL=agent.mjs.map
