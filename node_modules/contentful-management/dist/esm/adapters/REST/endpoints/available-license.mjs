import { get } from './raw.mjs';

const getBaseUrl = (params) => `/organizations/${params.organizationId}/available_licenses`;
const getMany = (http, params) => {
    return get(http, getBaseUrl(params), {
        params: params.query,
    });
};

export { getMany };
//# sourceMappingURL=available-license.mjs.map
