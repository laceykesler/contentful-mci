import { post } from './raw.mjs';

const get = (http, params, data, headers) => {
    return post(http, `/spaces/${params.spaceId}/environments/${params.environmentId}/semantic/search`, data, { headers });
};

export { get };
//# sourceMappingURL=semantic-search.mjs.map
