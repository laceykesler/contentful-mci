import { get as get$1, post } from './raw.mjs';

const AgentRunAlphaHeaders = {
    'x-contentful-enable-alpha-feature': 'agents-api',
};
const get = (http, params, headers) => {
    return get$1(http, `/spaces/${params.spaceId}/environments/${params.environmentId}/ai/agents/runs/${params.runId}`, {
        headers: {
            ...AgentRunAlphaHeaders,
            ...headers,
        },
    });
};
const getMany = (http, params, headers) => {
    return get$1(http, `/spaces/${params.spaceId}/environments/${params.environmentId}/ai/agents/runs`, {
        params: params.query,
        headers: {
            ...AgentRunAlphaHeaders,
            ...headers,
        },
    });
};
const resumeRun = (http, params, data, headers) => {
    return post(http, `/spaces/${params.spaceId}/environments/${params.environmentId}/ai/agents/runs/${params.runId}/resume`, data, {
        headers: {
            ...AgentRunAlphaHeaders,
            ...headers,
        },
    });
};

export { get, getMany, resumeRun };
//# sourceMappingURL=agent-run.mjs.map
