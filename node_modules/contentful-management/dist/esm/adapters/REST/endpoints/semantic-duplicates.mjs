import { post } from './raw.mjs';

const get = (http, params, data, headers) => {
    return post(http, `/spaces/${params.spaceId}/environments/${params.environmentId}/semantic/duplicates`, data, { headers });
};

export { get };
//# sourceMappingURL=semantic-duplicates.mjs.map
