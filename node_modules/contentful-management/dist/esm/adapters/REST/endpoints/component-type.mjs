import { get } from './raw.mjs';

const getBaseUrl = (params) => `/spaces/${params.spaceId}/environments/${params.environmentId}/component_types`;
const getMany = (http, params, headers) => {
    return get(http, getBaseUrl(params), {
        params: params.query,
        headers,
    });
};

export { getMany };
//# sourceMappingURL=component-type.mjs.map
