import { get } from './raw.mjs';

const getBaseUrl = (params) => `/spaces/${params.spaceId}/eligible_licenses`;
const getMany = (http, params) => {
    return get(http, getBaseUrl(params), {
        params: params.query,
    });
};

export { getMany };
//# sourceMappingURL=eligible-license.mjs.map
