import { get, put } from './raw.mjs';
import { normalizeSelect } from './utils.mjs';

const getMany = (http, params) => {
    return get(http, `/spaces/${params.spaceId}/space_add_ons`, {
        params: normalizeSelect(params.query),
    });
};
const updateAllocations = (http, params, data, headers) => {
    return put(http, `/spaces/${params.spaceId}/space_add_ons`, data, {
        headers,
    });
};

export { getMany, updateAllocations };
//# sourceMappingURL=space-add-on.mjs.map
