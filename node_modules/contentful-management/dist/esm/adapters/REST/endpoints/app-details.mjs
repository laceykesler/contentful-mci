import { del as del$1, get as get$1, put } from './raw.mjs';

const get = (http, params) => {
    return get$1(http, `/organizations/${params.organizationId}/app_definitions/${params.appDefinitionId}/details`);
};
const upsert = (http, params, data) => {
    return put(http, `/organizations/${params.organizationId}/app_definitions/${params.appDefinitionId}/details`, data);
};
const del = (http, params) => {
    return del$1(http, `/organizations/${params.organizationId}/app_definitions/${params.appDefinitionId}/details`);
};

export { del, get, upsert };
//# sourceMappingURL=app-details.mjs.map
